const FunComp = (props) =>{
    return (
        <div>
            <h2>Function Component</h2>
            <p>My Name is {props.fname} working as {props.post}</p>
        </div>
    );
}

export default FunComp;