const SaiFuncComp = (props) =>{
    return <h1>My Name is {props.fname} {props.lname} / Salary : {props.salary} / Gender : {props.gender} / Address : {props.address}</h1>
}

export default SaiFuncComp;